fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
redis[asyncio]>=5.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# Pin to 2.x — the 3.x release may rename fields (chain_id vs chainId).
# Review CHANGELOG before upgrading past 2.x.
siwe>=2.1.0,<3.0.0
